apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: ebed
spec:
  rules:
  -   host: titok.k8z.eu
      http:
        paths:
        - backend:
            serviceName: titok
            servicePort: 80
